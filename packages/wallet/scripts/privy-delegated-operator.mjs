/*
 * Stable bundled operator entry point. The implementation lives in the
 * adjacent module so the callback surface remains easy to exercise with a
 * mocked Privy provider transport.
 */
export * from "./privy-delegated-operator-v2.mjs";
