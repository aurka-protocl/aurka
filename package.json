{
  "name": "aurka",
  "version": "0.0.0",
  "private": true,
  "description": "Agentic portfolio-constrained liquidity protocol",
  "license": "UNLICENSED",
  "type": "module",
  "packageManager": "pnpm@10.13.1",
  "engines": {
    "node": ">=23.3.0 <24"
  },
  "scripts": {
    "build": "pnpm --filter @aurka/trader-app... --filter @aurka/services... --filter @aurka/graph... --filter @aurka/wallet... --filter @aurka/watchtower... build",
    "clean": "pnpm -r --if-present clean",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "integration:browser-smoke": "pnpm build && pnpm --filter @aurka/services exec node scripts/browser-smoke.mjs",
    "integration:local-settlement": "pnpm build && forge build && pnpm --filter @aurka/services exec node scripts/local-settlement-e2e.mjs",
    "integration:graph-node": "pnpm build && forge build && pnpm --filter @aurka/graph subgraph:build && pnpm --filter @aurka/services exec node scripts/local-graph-node-e2e.mjs",
    "integration:fork-real": "pnpm build && forge build && pnpm --filter @aurka/graph subgraph:build && node --env-file=.env packages/services/scripts/fork-real-release-e2e.mjs",
    "integration:fork-swapvm": "pnpm build && forge build && pnpm contracts:build-upstream && pnpm --filter @aurka/graph subgraph:build && node --env-file=.env packages/services/scripts/fork-swapvm-e2e.mjs",
    "contracts:build-upstream": "forge build contracts/upstream/AurkaUpstreamAquaSwapVMRouter.sol --use 0.8.30 --skip test --out contracts/out",
    "lint": "eslint .",
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck",
    "fork:start": "pnpm build && forge build && pnpm contracts:build-upstream && node --env-file=.env packages/services/scripts/fork-space.mjs",
    "fork:reset": "pnpm build && forge build && pnpm contracts:build-upstream && node --env-file=.env packages/services/scripts/fork-space.mjs --reset",
    "integration:fork-wallet": "node packages/services/scripts/fork-wallet-e2e.mjs",
    "privy:delegated": "node --env-file-if-exists=.env packages/wallet/scripts/privy-delegated-wallet.mjs",
    "app:dev": "pnpm --filter @aurka/trader-app dev --host 127.0.0.1 --strictPort",
    "app:preview": "pnpm --filter @aurka/trader-app preview --host 127.0.0.1 --strictPort"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "eslint": "10.10.0",
    "playwright": "1.55.0",
    "prettier": "3.9.6",
    "typescript": "5.9.3",
    "typescript-eslint": "8.69.0"
  }
}
